import 'package:flutter/material.dart';
// Screen Placeholder - Same name as main.dart expects

class TextTranslationScreen extends StatelessWidget {
  const TextTranslationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Text Translation - Coming Soon')),
      body: const Center(child: Text('Feature coming soon...')),
    );
  }
}
