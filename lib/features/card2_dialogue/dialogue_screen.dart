import 'package:flutter/material.dart';
// Screen Placeholder

class DialogueTranslationScreen extends StatelessWidget {
  const DialogueTranslationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dialogue Translation - Coming Soon')),
      body: const Center(child: Text('Feature coming soon...')),
    );
  }
}
