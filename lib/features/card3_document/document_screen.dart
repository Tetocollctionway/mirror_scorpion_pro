import 'package:flutter/material.dart';
// Screen Placeholder

class DocumentTranslationScreen extends StatelessWidget {
  const DocumentTranslationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Document Translation - Coming Soon')),
      body: const Center(child: Text('Feature coming soon...')),
    );
  }
}
